[project]
name = "pyproject-broken-target"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["this-package-definitely-does-not-exist-9x7z"]

[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main"]
